#s represents the two string sequences that will be compared to each other

sOne = ''
sTwo = ''

#The function below is simply to remove any spaces from pasting the data into the program

sOne = sOne.replace(' ', '')
sTwo = sTwo.replace(' ','')
count = 0

#In this for loop, 1 is added to the variable count when characters in the sequences are different from each other
for i in range(len(sOne)):
    if sOne[i] != sTwo[i]:
        count += 1

#The Hamming distance is then put to output

print(count)
